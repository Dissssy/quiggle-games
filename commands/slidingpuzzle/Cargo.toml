[package]
name = "qg-slidingpuzzle"
version = "0.4.2"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
qg-shared = { path = "../../shared" }
serde = { version = "1.0.192", features = ["derive"] }
